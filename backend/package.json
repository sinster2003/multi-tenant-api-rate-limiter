{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc -b && node --env-file=.env dist/index.js",
    "dev": "tsc -b && node --watch --env-file=.env dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
